<div class="mb-14 md:col-6 lg:col-4">
  <div class="glass rounded-lg p-4 text-center intersect:animate-fadeUp opacity-0">
    {
      image && (
        <a href={`/authors/${entry.id}`}>
          <Image
            class="mx-auto rounded" 
            src={image}
            alt={imageAlt}
            width={120}
            height={120}
            loading="eager"
          />
        </a>
      )
    }
    
    {/* Hemos quitado el h4, el p y el componente Social */}
  </div>
</div>